#ifndef ELEMENTO_H
#define ELEMENTO_H
#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

typedef int Telem;

Telem cargar_uno(void);
void mostrar_uno(Telem);
int retornar_num(Telem);
int es_primo(Telem);
int es_par(Telem);


#endif
